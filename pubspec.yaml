name: masjid_locator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  firebase_core: 
  firebase_auth: 
  cloud_firestore:
  firebase_storage: 

  firebase_analytics:
  google_fonts: 
  delightful_toast: ^1.1.0 

  image_picker: 
  provider: ^6.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
